# Feste Versionen, damit der Runner nicht still auf eine neue
# Hauptversion springt (so ist pandas 3.0 die Congress-Quelle
# kaputtgegangen). Updates kommen per Dependabot als Pull Request.
requests==2.34.2
feedparser==6.0.14
pandas==3.0.5
lxml==6.1.3
tzdata==2026.4
